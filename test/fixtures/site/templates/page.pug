extends layout

block content
  article
    h1= page.title
    p.meta filename: #{page.filename}
    p.meta url: #{page.url}
    p.meta date: #{page.rfc822date}
    p.meta hasMore: #{page.hasMore}
    section.intro!= page.intro
    section.body!= page.html
